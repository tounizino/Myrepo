(function($){
    $(function(){
        // Initialize color pickers
        if ($.fn.wpColorPicker) {
            $('.unpc-color-picker').wpColorPicker();
        }

        // Add live preview or additional functionality
        console.log('Ultimate NAT & Port Checker Admin Panel Loaded');
    });
})(jQuery);
